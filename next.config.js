/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "bookshopapi.assamtechnologies.com"],
  },
};

module.exports = nextConfig;
